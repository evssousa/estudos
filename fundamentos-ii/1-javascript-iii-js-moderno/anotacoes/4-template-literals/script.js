/*
- Template literals é outra forma de declara string;
- Utiliza o acento grave (crase);
- Permite interpolação de strings e quebra de linha automática;
*/

// Exemplo:
let str = `Essa linha será
quebrada automaticamente
  e essa quebrará e terá um espaço no começo
Resultado de uma expressão: ${2 + 2}`

const nome = 'Everson'
console.log(`Olá mundo, ${nome}`)