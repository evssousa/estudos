/**
 * Servem para armazenar os dados para podermos usá-los no nosso código.
 * Criando variáveis:
 *  - var: variável global
 *  - let: variável local
 *  - const: variável imutável, não modifica seu valor
 * Nomeando variáveis:
 *  - Nomes significativos: escrever o nome da variável tendo algum significado nela.
 *  - Cases: ex: nome-completo, ou, nomeCompleto)
 */

// Exemplos
var nome = 'Everson'

// nome agora recebe o valor 'Everson', que é uma string